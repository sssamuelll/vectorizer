opencv-contrib-python>=4.8
numpy>=1.24
# vtracer: wheels verificados solo en Windows / Python 3.14 (2026-06-05).
# En otras plataformas pip puede intentar compilar desde fuente (Rust).
vtracer>=0.6.15
# dev
pytest>=8
# fontid (aproximación de fuentes):
# winocr usa el OCR nativo de Windows (winrt) — Windows-only; además
# requiere un language pack OCR de script latino instalado en el SO.
winocr
# anthropic: SOLO para fontid --api (nominación opt-in). Opcional.
anthropic
fonttools>=4.60          # recompose.py: outlines de glifos desde TTF
# resvg_py — OPCIONAL: solo el preview de recompose.py (pip install resvg_py)
